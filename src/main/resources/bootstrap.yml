spring:
  application:
    name: PlanMS
  cloud:
    consul:
      discovery:
        hostname: localhost
      config:
        enabled: true
        prefix: config
        defaultContext: application
        profile-separator: '::'
        format: YAML

server:
  port: 8400